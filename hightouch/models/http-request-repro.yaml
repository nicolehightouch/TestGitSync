name: HTTP Request repro
source: mongo-db-9ngvc
type: custom
customQuery:
  type: mongodb
  filter: |-
    {
      "kind": "cloud"
    }
  database: Nicoles_sync
  collection: Nicoles_sync_collection
primaryKey: _id
