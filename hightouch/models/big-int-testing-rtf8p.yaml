name: big int testing
source: success-eng---postgres
type: raw_sql
rawSql: >-
  Select ID, RECEIVED_AT, FIRST_NAME, LAST_NAME, cast(lifetime_value as
  smallint) from warby_parker_demo.users
primaryKey: id
