model: slack-test-fhepw
destination: nd-slack-destination-4y0gu
config:
  mode: message
  channel: C036WC1G0E4
  addedBody: "*New Breaking Change: {{ connector_name }}*\n\nThe {{ connector_type }},\_{{ connector_name }}, has a new `<``{{ migration_documentation_url }}``|breaking change>` with the version {{ breaking_change_connector_version }}\_and an upgrade deadline of {{ upgrade_deadline }}. \n\n Message: _{{ breaking_change_message }}_"
  isBlockKit: false
  configVersion: 1
schedule:
  type: manual
schedulePaused: false
